{
  "name": "one-tool-gpuix",
  "version": "0.1.0",
  "private": true,
  "description": "GPUIX desktop client for One Tool. Windows DirectX + Ubuntu 24.04 Vulkan. Talks to converter/server.py; does not replace Electron.",
  "type": "module",
  "scripts": {
    "dev": "bun --hot app.tsx",
    "start": "bun app.tsx",
    "doctor": "bun scripts/check-runtime.ts",
    "build": "bun build --compile app.tsx --outfile dist/one-tool",
    "build:linux": "bun scripts/build-native.ts linux",
    "build:windows": "bun scripts/build-native.ts windows",
    "pack:linux": "bun run build:linux && cargo packager --release --config packager.linux.json",
    "pack:windows": "bun run build:windows && cargo packager --release --config packager.windows.json",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@gpuix/native": "0.9.0",
    "@gpuix/react": "0.9.0",
    "react": "^19.2.4"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "@types/react": "^19.2.0",
    "typescript": "^5.9.2"
  }
}
